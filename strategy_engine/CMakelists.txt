# strategy_engine/CMakeLists.txt
add_library(strategy_engine INTERFACE) # Make sure this line defines INTERFACE

# Add source files when they exist
# target_sources(strategy_engine PRIVATE
#    src/strategy.cpp
#    src/rule.cpp
# )

# Ensure the following commands use INTERFACE
target_include_directories(strategy_engine INTERFACE include)
target_compile_features(strategy_engine INTERFACE cxx_std_20)
target_link_libraries(strategy_engine INTERFACE core)

message(STATUS "Configuring strategy_engine module...")